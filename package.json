{
  "name": "express-gateway-test",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "prism validate --spec spec.json --upstream http://localhost:8080"
  },
  "dependencies": {
    "express-gateway": "^1.13.0",
    "console.table": "^0.10.0"
  },
  "engines": {
    "node": "10.x"
  },
  "license": "MIT"
}
